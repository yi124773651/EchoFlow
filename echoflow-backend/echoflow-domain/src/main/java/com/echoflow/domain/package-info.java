/**
 * EchoFlow domain model.
 *
 * <p>This module is <strong>pure Java</strong>: no Spring, no JPA, no HTTP,
 * no AI SDK.  It owns business rules, entities, value objects, domain events,
 * and repository interfaces (port definitions).</p>
 */
package com.echoflow.domain;
